import { Suspense } from 'react';
import { Form } from '@/components/Form/Form';

export default function SignInPage() {
    return (
        <Suspense>
            <Form />
        </Suspense>
    );
}
