import { styled } from '@mui/material/styles';

export const ResendVerificationSubTitle = styled('label')({
    display: 'flex',
    justifyContent: 'center',
    color: 'green',
    fontSize: '16px',
    lineHeight: '22px',
});
